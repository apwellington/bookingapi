spring:
  config:
    activate:
      on-profile: dev
  jpa:
    hibernate:
      ddl-auto: create

  datasource:
    url: jdbc:h2:mem:bookingdb
    username: sa
    password: password
    driverClassName: org.h2.Driver
---

